# Python dependencies for DC Bike Weather
# Install with:  pip install -r requirements.txt
 
# --- Core: required to run dc_bike_weather.py (the PDF script) ---
requests>=2.25
numpy>=1.21
pandas>=1.5
matplotlib>=3.5
 
# --- Report: also required to render dc_bike_weather_report.qmd ---
# Quarto executes the report's Python cells through Jupyter.
# Quarto itself, and TinyTeX for PDF output, are installed separately
# (see the README): a Quarto CLI install, then `quarto install tinytex`.
jupyter>=1.0
